<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>defer与异常处理</title>
    <link href="/2023/11/07/defer%E4%B8%8E%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/"/>
    <url>/2023/11/07/defer%E4%B8%8E%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="defer与异常处理"><a href="#defer与异常处理" class="headerlink" title="defer与异常处理"></a><code>defer</code>与异常处理</h1><p>在本篇文章中，介绍关于Go语言中<code>defer</code>的使用以及注意事项，还有如何在Go语言中使用<code>defer</code>去处理异常。<code>defer</code>是Go语言提供的关键字，它的作用是，当你声明了注册了<code>defer</code>语句，那么该语句会在函数<strong>返回时</strong>被执行。</p><h2 id="为什么要使用defer"><a href="#为什么要使用defer" class="headerlink" title="为什么要使用defer"></a>为什么要使用<code>defer</code></h2><p>在<code>C\C++</code>或者其它需要手动管理某些资源（包括但不限于内存、文件描述符等）的语言中，我们可能会存在以下情况，那就是我们开辟了一块新的内存，或者打开了一个新的文件描述符，刚开始我们可能记着要释放，但是写代码写着写着，写到最后我们可能就忘了有这么一回事了，这样就可能导致资源泄露，所以各种语言都提供了一种或者多种机制，用于在代码块结束、函数调用结束、变量生命周期结束的时机释放资源。例如C++提供了RAII用于当前对象声明周期结束后使用析构函数释放资源、Rust提供了生命周期的方式用来保证资源正确释放，而<code>defer</code>就是Go语言提供的用于释放资源的方式。</p><p><code>defer</code>在Go语言中，可以用于释放互斥锁、增加<code>waitGroup</code>的计数、回滚数据库事务以及处理各种函数调用的<code>Epilogue</code>。虽然是手动的，但是功能非常强大。</p><h2 id="怎样使用defer"><a href="#怎样使用defer" class="headerlink" title="怎样使用defer"></a>怎样使用<code>defer</code></h2><p><code>defer</code>的基础使用方式如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample1</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;deferExample1&#x27;s defer Invoke!&quot;</span>)<br>fmt.Println(<span class="hljs-string">&quot;deferExample1 Invoke!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107220137706.png" alt="image-20231107220137706"></p><p>可以看到实现执行了函数内部的其他代码，最后才执行的<code>defer</code>语句，<code>defer</code>关键字之后不仅可以跟一条语句，还可以跟一个函数的调用语句，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample2</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;deferExample2&#x27;s defer function Invoke!&quot;</span>)<br>&#125;()<br>fmt.Println(<span class="hljs-string">&quot;deferExample2 Invoke!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107220524459.png" alt="image-20231107220524459"></p><p>并且在一个函数中可以使用<code>defer</code>关键字注册多个函数的调用语句，或者普通语句，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample3</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;deferExample3&#x27;s defer statement 1 execute!&quot;</span>)<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;deferExample3&#x27;s defer function 2 execute!&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;deferExample3&#x27;s defer function 3 execute!&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;deferExample3&#x27;s defer statement 4 execute!&quot;</span>)<br>fmt.Println(<span class="hljs-string">&quot;deferExample3 Invoke!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107220939503.png" alt="image-20231107220939503"></p><p>这里我们可以看到，<code>defer</code>注册语句的执行顺序与注册顺序相反，我们是按照1,2,3,4的顺序注册的，而调用是按照4,3,2,1的顺序调用的。这是因为在Go语言的运行时在函数执行期间维护了一个“链栈”，每次调用<code>defer</code>就会在这个“链栈”中新增一项，而我们知道栈这种数据结构具有FILO（First In Last Out）的特性的，所以最先注册的<code>defer</code>语句会被最后调用，具体的过程如下所示：</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107222133876.png" alt="image-20231107222133876"></p><p>这张图清晰的解释了<code>defer</code>的整个工作流程，可以看到<code>defer</code>语句是如何注册，如何执行的。并且还有关键的一点就是，<code>derfer</code>并不是在函数返回完成之后执行的，<strong>而是在函数返回期间执行的</strong>，因为函数的返回并不是一个原子操作，返回期间要做很多事情，比如处理函数栈帧时，就要释放局部变量、给返回值寄存器赋值等等，所以<code>defer</code>语句也是在返回期间完成的。</p><h2 id="使用defer的注意事项"><a href="#使用defer的注意事项" class="headerlink" title="使用defer的注意事项"></a>使用<code>defer</code>的注意事项</h2><p>在面试的时候关于<code>defer</code>关键字，除了上述的执行流程，一般还会有给你一段代码让你说出代码的执行结果这种问题，所以我们也来看看关于<code>defer</code>使用的细枝末节。</p><h3 id="预计算参数"><a href="#预计算参数" class="headerlink" title="预计算参数"></a>预计算参数</h3><p>在使用<code>defer</code>时，这是一个比较常见的场景，就是在<code>defer</code>中使用一个当前函数的变量，以一个示例来了解：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample4</span><span class="hljs-params">()</span></span> &#123;<br>i := <span class="hljs-number">5</span><br><span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;deferExample4&#x27;s defer i = &quot;</span>, i)<br>i = <span class="hljs-number">10</span><br>fmt.Println(<span class="hljs-string">&quot;deferExample4&#x27;s i = &quot;</span>, i)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107223215404.png" alt="image-20231107223215404"></p><p>我们可以看到这里函数内部打印的值为10，<code>defer</code>语句中打印的值为5，因为<code>defer</code>语句具有预计算参数的作用，意思就是我们在注册<code>defer</code>语句的时候，<code>defer</code>语句中的值就已经被计算出来了，所以<code>defer</code>语句中的<code>i</code>在注册时就被赋值了。</p><p>这种情况，与我们使用<code>defer</code>进行函数调用时，将函数体内部的变量以参数的方式传递到<code>defer</code>注册的函数中时一致，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample5</span><span class="hljs-params">()</span></span> &#123;<br>i := <span class="hljs-number">5</span><br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(i <span class="hljs-type">int</span>)</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;deferExample5&#x27;s defer i = &quot;</span>, i)<br>&#125;(i)<br>i = <span class="hljs-number">10</span><br>fmt.Println(<span class="hljs-string">&quot;deferExample5&#x27;s i = &quot;</span>, i)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107223834078.png" alt="image-20231107223834078"></p><p>但是在<strong>使用闭包捕获函数体内的变量到<code>defer</code>注册的函数中</strong>时，情况就会发生变化，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample6</span><span class="hljs-params">()</span></span> &#123;<br>i := <span class="hljs-number">5</span><br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;deferExample6&#x27;s defer i = &quot;</span>, i)<br>&#125;()<br>i = <span class="hljs-number">10</span><br>fmt.Println(<span class="hljs-string">&quot;deferExample6&#x27;s i = &quot;</span>, i)<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107224112457.png" alt="image-20231107224112457"></p><p>在这里我们得到了相同的输出，这是因为<code>defer</code>函数在注册时将该变量捕获到函数内部了，而<code>defer</code>语句是在函数返回期间执行的，这里<code>defer函数</code>内部的<code>i</code>和函数内部的<code>i</code>指向的是同一个变量地址，所以<code>defer函数</code>会和函数打印同样的值。</p><p>不过在这里还有一种特殊情况，那就是当返回值是一个命名返回值时，它又会出现不同的结果，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deferExample7</span><span class="hljs-params">()</span></span> (i <span class="hljs-type">int</span>) &#123;<br>i = <span class="hljs-number">5</span><br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Printf(<span class="hljs-string">&quot;deferExample7&#x27;s defer i = %d,addr = %p\n&quot;</span>, i, &amp;i)<br>&#125;()<br>i = <span class="hljs-number">10</span><br>fmt.Printf(<span class="hljs-string">&quot;\&quot;deferExample7&#x27;s  i = %d,addr = %p\n&quot;</span>, i, &amp;i)<br>i = <span class="hljs-number">3</span><br><span class="hljs-keyword">return</span> i + <span class="hljs-number">2</span><br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107225428455.png" alt="image-20231107225428455"></p><p>如果根据上面的闭包规则，那么我们预期得到的结果应该是3，因为在最后3被赋予了i，但是由于这是一个命名返回参数，而<code>defer</code>语句又是在返回期间执行的，所以我们得到了5，用下面这张图来解释这个现象：</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231107230151521.png" alt="image-20231107230151521"></p><p>这样我们就清晰的知道，为什么用闭包捕获一个命名返回值会出现这种不同的作用了，这是由于<code>defer</code>语句是在函数返回期间执行的，而<code>defer</code>语句又在注册的时候闭包捕获了命名返回值i获得了i的地址，所以函数在返回的时候将返回值赋给命名返回值，此时命名返回值i的值就发生了变化，而赋予返回值之后，才开始执行函数调用的清理工作，比如释放栈帧和执行<code>defer</code>语句，所以<code>defer</code>语句中使用的命名返回值是最新的值5。</p><p>还有一点就是，我们在使用<code>defer</code>的时候，不能只在<code>defer</code>关键字之后定义一个匿名函数，而不调用它，这样是错误的。</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231109200855177.png" alt="image-20231109200855177"></p><p>综上所述，我们使用<code>defer</code>时要注意如下几点：</p><ul><li><p><code>defer</code><strong>关键字之后只能跟一条语句，而不能跟一个匿名函数的定义！</strong></p></li><li><p><code>defer</code>语句是在函数返回过程中执行的，而不是函数返回后执行的。</p></li><li><p><code>defer</code>语句具有预计算值的功能，当我们没有使用闭包捕获函数内部的变量时，<code>defer</code>语句在注册时就会计算出要使用的值。</p></li><li><p><code>defer</code>函数闭包捕获函数内部的变量，在<code>defer</code>函数执行时会获取到该变量最新的值，因为<code>defer</code>函数获取了该变量的地址。</p></li><li><p><code>defer</code>函数闭包捕获命名返回值时，如果函数内部<strong>显示指定</strong>了返回值，那么这个<strong>返回值会作为命名返回值最新值</strong>，<code>defer</code>函数在执行时也会使用最新的值。</p></li></ul><h2 id="什么是异常和错误"><a href="#什么是异常和错误" class="headerlink" title="什么是异常和错误"></a>什么是异常和错误</h2><p>Go语言中的异常与错误和其他语言中的异常与错误有一定的区别，所以我们主要来了解在Go语言中什么是异常和错误，简单的来说，Go语言中的异常就是程序在运行过程中由<code>panic</code>引发异常，这种异常如果没有恢复则会导致程序崩溃，而Go语言中的错误就是实现了<code>error</code>接口的类型，我们可以在函数的返回值中返回一个实现了<code>error</code>接口的值，用于告诉函数的调用者，当前函数是否正常执行，如果没有正常执行，那么错误信息是什么。</p><h2 id="怎样处理异常和错误"><a href="#怎样处理异常和错误" class="headerlink" title="怎样处理异常和错误"></a>怎样处理异常和错误</h2><p>在Go语言中，处理异常和错误我们一般使用以下的两种方式。</p><p>对于错误（error），我们一般是这样处理的：</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/DCIF7-2W0AEAv9c.jpg" alt="Copypasting &quot;if err!= nil {return err;} everywhere : r/golang"></p><p>咳咳，正经的说，因为Go语言中的error是一个接口，所以返回的错误总是一个值，我们可以通过对这个值进行判断来了解当前发生了什么错误，并且知道该如何处理该错误。例如：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">if</span> err == ErrSomething &#123; … &#125;<br></code></pre></td></tr></table></figure><p>在这里，如果当前发生的错误是<code>ErrorSomething</code>我们就执行对该错误的处理，如果是其他错误，也是一样的。但是我们最常用的，还是上面这张图中的代码（每个Gopher都需要这样一个键盘！），因为我们一般不会在当前函数中处理当前函数发生的错误，一般是将错误传播回去，由上层来决定如何处理。</p><p>而对于异常，也就是由<code>panic</code>引发的，异常一般会导致程序崩溃，这代表发生了非常严重的错误，如果在开发环境中我们一般不会去处理该错误，而是借助该错误来Debug，如果是线上的运行环境，我们担心发生了panic，那我们一般会使用<code>recover</code>函数来捕获这个<code>panic</code>然后将其当做一个错误传递出去，具体的示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">mayPanic</span><span class="hljs-params">()</span></span>&#123;<br>    <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;a problem&quot;</span>)<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span>&#123;<br>        <span class="hljs-keyword">if</span> err:=<span class="hljs-built_in">recover</span>();err!=<span class="hljs-literal">nil</span>&#123;<br>            fmt.Println(err)<br>        &#125;<br>    &#125;()<br>    mayPanic()<br>&#125;<br></code></pre></td></tr></table></figure><p>在这段代码中，我们使用了<code>recover</code>来捕获发生的<code>panic</code>然后将其打印出来。在实际的环境中，我们也可以使用这样的方式，来防止程序崩溃。</p><p>在Go语言中对于异常和错误的处理，一般情况就是以上的两种方式。</p><h2 id="处理异常和错误的注意事项"><a href="#处理异常和错误的注意事项" class="headerlink" title="处理异常和错误的注意事项"></a>处理异常和错误的注意事项</h2><p>除了以上的方式，还有一些特殊的情况需要了解，接下来我们来看一个特殊的例子，那就是在<code>defer</code>执行的函数中发生了<code>panic</code>会怎么样呢？示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">mayPanic</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 1&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;I&#x27;ll panic!&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 2&quot;</span>)<br>&#125;()<br>fmt.Println(<span class="hljs-string">&quot;mayPanic Invoke&quot;</span>)<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;main function defer invoke&quot;</span>)<br>&#125;()<br>mayPanic()<br>fmt.Println(<span class="hljs-string">&quot;main function invoke!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>对于这段代码，运行的结果如下：</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231109202428036.png" alt="image-20231109202428036"></p><p>我们再来看一个例子，对比着看：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">mayPanic</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 1&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;I&#x27;ll panic!&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 2&quot;</span>)<br>&#125;()<br><span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;It&#x27;s problem&quot;</span>)<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span>&#123;<br>        fmt.Println(<span class="hljs-string">&quot;may Panic 3&quot;</span>)<br>    &#125;()<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;main function defer invoke&quot;</span>)<br>&#125;()<br>mayPanic()<br>fmt.Println(<span class="hljs-string">&quot;main function invoke!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>这个例子的输出如下：</p><p><img src="https://store-yakumo-image.oss-cn-hangzhou.aliyuncs.com/image/image-20231109202517472.png" alt="image-20231109202517472"></p><p>从以上的两个例子中，我们可以简单的得出一个结论，<strong>当一个函数内部发生了<code>panic</code>，如果在发生<code>panic</code>之前，函数内部注册了<code>defer</code>语句，那么<code>panic</code>会被推迟到所有<code>defer</code>语句执行完毕。如果注册了多个<code>defer</code>语句，这多个<code>defer</code>语句中有一个或者多个发生了<code>panic</code>，都不会影响剩下的<code>defer</code>语句执行，只是会将<code>panic</code>的信息记录下来，直到所有<code>defer</code>语句执行完毕，再和函数内部发生的<code>panic</code>一起返回到上层函数，如果上层函数没有捕获，那么该<code>panic</code>就会导致程序崩溃。</strong></p><p>至于为什么会发生这种情况，那当然是因为，我们要在<code>defer</code>语句中捕获<code>panic</code>，而编译器不知道哪个<code>defer</code>语句才会捕获，就只能全部执行。如果当前函数中没有注册<code>defer</code>语句，那么该函数就会直接将<code>panic</code>传播到上层。</p><p>而对于<code>defer</code>语句中发生的<code>panic</code>，如果需要捕获，那就在<code>defer</code>语句中套娃，示例如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">mayPanic</span><span class="hljs-params">()</span></span> &#123;<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 1&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span>&#123;<br>            <span class="hljs-keyword">if</span> err:=<span class="hljs-built_in">recover</span>();err!=<span class="hljs-literal">nil</span>&#123;<br>                fmt.Println(err)<br>            &#125;<br>        &#125;()<br>        <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;I&#x27;ll panic!&quot;</span>)<br>&#125;()<br><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>fmt.Println(<span class="hljs-string">&quot;may Panic 2&quot;</span>)<br>&#125;()<br><span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;It&#x27;s problem&quot;</span>)<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span>&#123;<br>        fmt.Println(<span class="hljs-string">&quot;may Panic 3&quot;</span>)<br>    &#125;()<br>&#125;<br></code></pre></td></tr></table></figure><p>关于<code>panic</code>的内容暂且就这么多，接下来看看关于<code>error</code>的内容，这一部分我们主要了解，除了：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">if</span> err!=<span class="hljs-literal">nil</span>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>,err<br>&#125;<br></code></pre></td></tr></table></figure><p>之外的处理方式，比如自定义错误，前面我们说了，在Go语言中，只要一个类型实现了<code>error</code>接口，那么该类型就可以当做一个错误使用，比如：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> MyError <span class="hljs-keyword">struct</span>&#123;<br>    name <span class="hljs-type">string</span><br>    age <span class="hljs-type">int</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(m *MyError)</span></span> Error() <span class="hljs-type">string</span> &#123;<br><span class="hljs-keyword">return</span> m.name + <span class="hljs-type">string</span>(m.age)<br>&#125;<br></code></pre></td></tr></table></figure><p>这样我们就自定义了一个错误， 当我们需要使用的时候，就可以将这个类型，当做一个错误来进行处理。</p>]]></content>
    
    
    <categories>
      
      <category>GO</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Go</tag>
      
      <tag>学习笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ARTS_1</title>
    <link href="/2023/10/28/ARTS-1/"/>
    <url>/2023/10/28/ARTS-1/</url>
    
    <content type="html"><![CDATA[<h1 id="ARTS-1"><a href="#ARTS-1" class="headerlink" title="ARTS_1"></a>ARTS_1</h1><h2 id="Algorithm"><a href="#Algorithm" class="headerlink" title="Algorithm"></a>Algorithm</h2><p>Leetcode 9.回文数</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br>  <span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">isPalindrome</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (x == <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">if</span> (x &lt; <span class="hljs-number">0</span> || x % <span class="hljs-number">10</span> == <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-type">int</span> rev = <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">while</span> (x &gt; rev) &#123;<br>            rev = rev * <span class="hljs-number">10</span> + x % <span class="hljs-number">10</span>;<br>            x /= <span class="hljs-number">10</span>;<br>        &#125;<br>        <span class="hljs-comment">// 处理奇数个数的数字，如果 x&lt;rev 就说明 输入的 x 是奇数位数字</span><br>        <span class="hljs-keyword">if</span> (x &lt; rev) &#123;<br>            x = x * <span class="hljs-number">10</span> + rev % <span class="hljs-number">10</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> x == rev;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>有两种思路，第一种是将数字转换为字符串，然后一一比对，第二种思路是翻转数字的一半，比较翻转的一半和未翻转的一半是否相等，如果相等那么就是回文的。<br>使用第二种方法要注意如下几点：</p><ol><li>如果数字为0，那么是回文的。</li><li>如果数字为负数或者数字最后一位是0（因为数字的最高位不可能为0），那么不是回文的。</li><li>如果数字的个数是奇数位，那么在循环翻转完成之后，给<code>x</code>加上一个<code>rev</code>的最低位，这样就可以保证翻转后的数字和原数字的数字个数相等。</li><li>最后结果x与rev相等则是回文，否则不是。</li></ol><h2 id="Tips"><a href="#Tips" class="headerlink" title="Tips"></a>Tips</h2><p><code>JavaScript</code>中，函数类型具有一个方法<code>toString()</code>，使用该方法可以获得函数内部的源码，包括函数内部的注释，利用这个方法，我们可以以一种不同的方式来实现多行字符串。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs JavaScript">(<span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123; <span class="hljs-comment">/*</span><br><span class="hljs-comment">line 1</span><br><span class="hljs-comment">line 2</span><br><span class="hljs-comment">line 3</span><br><span class="hljs-comment">*/</span>&#125;).<span class="hljs-title function_">toString</span>().<span class="hljs-title function_">split</span>(<span class="hljs-string">&#x27;\n&#x27;</span>).<span class="hljs-title function_">slice</span>(<span class="hljs-number">1</span>, -<span class="hljs-number">1</span>).<span class="hljs-title function_">join</span>(<span class="hljs-string">&#x27;\n&#x27;</span>)<br></code></pre></td></tr></table></figure><p>这段代码的返回值就是里面的注释，所以可以直接拿到如下结果</p><blockquote><p>line 1</p><p>line 2</p><p>line 3</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>ARTS</category>
      
    </categories>
    
    
    <tags>
      
      <tag>ARTS</tag>
      
      <tag>算法</tag>
      
      <tag>编程技巧</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
